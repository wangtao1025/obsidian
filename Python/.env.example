# Supabase 复习状态跨设备同步（可选）
# 在 Supabase 控制台创建项目后，将下方替换为你的 Project URL 和 anon public key
# 复制本文件为 .env.local 并填写实际值；不配置则仅使用本地 localStorage
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
