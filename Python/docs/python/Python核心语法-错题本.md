# Python 核心语法 · 错题本

> 本页集中收录自测卷中标记为「待复习」的题目，**一键跳转到题目与知识点**，方便集中复习。

**使用方式**：从下表点「题目」进入自测卷作答，点「知识点」进入语法手册查看详解；复习后可取消该题在自测卷/手册中的待复习标记。

---

## 二、内置数据结构

### 2.1 列表五大特点 {#21-列表五大特点}

| 项目 | 链接 |
|------|------|
| **题目** | [列表的五大特点是什么？](/python/Python核心语法自测试卷#二内置数据结构)（自测卷 2.1） |
| **知识点** | [列表 (List) 特点：有序、可变、重复、索引与切片、元素任意类型](/python/python语法手册-02-内置数据结构#21-列表-list---有序可变容器) |
| **要点** | 有序；可变；支持重复元素；支持索引与切片；元素可为任意类型。 |

---

### 2.3 .pop() 与 .remove() 区别 {#23-pop-remove}

| 项目 | 链接 |
|------|------|
| **题目** | [.pop() 和 .remove() 的区别是什么？](/python/Python核心语法自测试卷#二内置数据结构)（自测卷 2.3） |
| **知识点** | [列表 关键操作 (删除)：.pop(i) 与 .remove(x)](/python/python语法手册-02-内置数据结构#21-列表-list---有序可变容器) |
| **要点** | **pop(i)**：按索引删除并返回该元素（默认最后一位）；i 越界或空列表 → `IndexError`。**remove(x)**：按值删除第一个匹配项，不返回值；x 不存在 → `ValueError`。 |

---

### 2.4 .sort() 与 sorted() 区别 {#24-sort-sorted}

| 项目 | 链接 |
|------|------|
| **题目** | [.sort() 和 sorted() 的核心区别是什么？](/python/Python核心语法自测试卷#二内置数据结构)（自测卷 2.4） |
| **知识点** | [sorted() 与 .sort() 详解（小白必读）](/python/python语法手册-02-内置数据结构#211-sorted-与-sort-详解小白必读) |
| **要点** | **.sort()**：原地排序，修改原列表，返回 `None`；仅用于列表。**sorted()**：返回新列表，不修改原数据；可排序任意可迭代对象。 |

---

### 2.9 元组两个内置方法 {#29-元组方法}

| 项目 | 链接 |
|------|------|
| **题目** | [元组有哪两个内置方法？](/python/Python核心语法自测试卷#二内置数据结构)（自测卷 2.9） |
| **知识点** | [元组 核心操作：.count(x) 与 .index(x)](/python/python语法手册-02-内置数据结构#22-元组-tuple---有序不可变序列) |
| **要点** | **.count(x)**：统计 `x` 出现次数，不存在返回 0 不报错。**.index(x)**：查找 `x` 第一次出现的索引，不存在 → `ValueError`。元组只有这两个方法。 |

---

### 2.16 .find() 与 .index() 区别 {#216-find-index}

| 项目 | 链接 |
|------|------|
| **题目** | [.find("sub") 和 .index("sub") 的区别是什么？](/python/Python核心语法自测试卷#25-字符串-string)（自测卷 2.16） |
| **知识点** | [字符串 查找：.find() 与 .index()](/python/python语法手册-02-内置数据结构#251-查找替换) |
| **要点** | **.find()**：找不到返回 `-1`，不报错。**.index()**：找不到抛出 `ValueError`。 |

---

### 2.19 .split() 无参 vs .split(" ") {#219-split}

| 项目 | 链接 |
|------|------|
| **题目** | [split() 和 split(" ") 的结果有什么不同？](/python/Python核心语法自测试卷#25-字符串-string)（自测卷 2.19） |
| **知识点** | [字符串 .split()：默认分隔符与 maxsplit](/python/python语法手册-02-内置数据结构#255-split) |
| **要点** | **.split()**（无参）：按任意空白字符分割，会合并连续空白。**.split(" ")**：严格按单个空格分割，连续空格会产生空字符串元素。 |

---

### 2.27 小整数对象池 {#227-小整数对象池}

| 项目 | 链接 |
|------|------|
| **题目** | [Python 的小整数对象池缓存范围是多少？](/python/Python核心语法自测试卷#26-整型-integer)（自测卷 2.27） |
| **知识点** | [整型 小整数对象池](/python/python语法手册-02-内置数据结构#26-整型-integer---不可变数值对象) |
| **要点** | 默认缓存 **[-5, 256]** 的整数，用于优化性能；同值可能同一对象（`id` 相同）。 |

---

### 2.30 浮点精度误差原因 {#230-浮点精度}

| 项目 | 链接 |
|------|------|
| **题目** | [浮点数存在精度误差的根本原因是什么？](/python/Python核心语法自测试卷#27-浮点型-float)（自测卷 2.30） |
| **知识点** | [浮点型 特点与精度](/python/python语法手册-02-内置数据结构#27-浮点型-float---双精度浮点数) |
| **要点** | 计算机用**二进制**存小数，很多十进制小数无法精确表示为有限位二进制，故产生精度误差。 |

---

## 三、异常捕获与主动抛出

### 3.5 UnicodeEncodeError 与 UnicodeDecodeError {#35-unicode}

| 项目 | 链接 |
|------|------|
| **题目** | [UnicodeEncodeError 和 UnicodeDecodeError 分别在什么操作中触发？](/python/Python核心语法自测试卷#三异常捕获与主动抛出-共-8-题)（自测卷 3.5） |
| **知识点** | [异常 编码/解码错误](/python/python语法手册-03-异常) |
| **要点** | **UnicodeEncodeError**：字符串→字节时失败（如 ASCII 编码中文）。**UnicodeDecodeError**：字节→字符串时失败（如用 UTF-8 解码 GBK 字节流）。 |

---

### 3.6 try-except-else-finally 中 else 何时执行 {#36-try-else}

| 项目 | 链接 |
|------|------|
| **题目** | [else 块在什么时候执行？](/python/Python核心语法自测试卷#三异常捕获与主动抛出-共-8-题)（自测卷 3.6） |
| **知识点** | [try-except-else-finally：else 与 finally](/python/python语法手册-03-异常) |
| **要点** | **else**：当 `try` 块**没有发生任何异常**时执行；若有异常被 `except` 捕获，else 不执行。 |

---

## 四、格式化输出 f-string 与 print

### 4.1 f-string 格式说明符完整语法 {#41-格式说明符}

| 项目 | 链接 |
|------|------|
| **题目** | [f-string 格式说明符的完整语法是什么？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.1） |
| **知识点** | [f-string 格式说明符](/python/python语法手册-04-格式化与输出) |
| **要点** | `{变量:填充字符对齐方式宽度.精度类型}`。如：`d` 十进制整数，`f` 浮点，`x`/`X` 十六进制，`<`/`>`/`^` 对齐。 |

---

### 4.2 格式化输出结果 {#42-格式化输出}

| 项目 | 链接 |
|------|------|
| **题目** | [写出以下格式化的输出结果](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.2） |
| **知识点** | [f-string 数字格式化、对齐、补零](/python/python语法手册-04-格式化与输出) |
| **要点** | `.2f`→保留 2 位小数；`010d`→宽 10 补零；`<10`/`>10`/`^10`→左/右/居中对齐。 |

---

### 4.4 自定义填充与默认补空格 {#44-自定义填充}

| 项目 | 链接 |
|------|------|
| **题目** | [自定义填充字符时为什么必须显式指定对齐方式？f"{n:1>10d}" 和 f"{n:110d}" 的区别？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.4） |
| **知识点** | [f-string 补零、默认填充、自定义填充](/python/python语法手册-04-格式化与输出) |
| **要点** | **默认**：不写填充字符时，Python 用**空格**补足宽度；**补零**：写 `0` 时（如 `:010d`）表示用 0 填充，不必再写对齐方式。**自定义其他字符**（如用 `1` 填充）：必须显式写对齐方式（如 `1>`），否则 `f"{n:110d}"` 会被解析为“宽度 110、默认空格填充”，而不是“用 1 填充、宽度 10”。 |

---

### 4.6 码点格式化为 U+4E2D {#46-码点}

| 项目 | 链接 |
|------|------|
| **题目** | [将字符 '中' 的 Unicode 码点格式化为 U+4E2D 的 f-string 代码](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.6） |
| **知识点** | [f-string 码点展示：ord + :04X](/python/python语法手册-04-格式化与输出) |
| **要点** | `f"U+{ord('中'):04X}"`。`ord('中')` 取码点（十进制），`:04X` 表示 4 位十六进制大写、不足补 0。 |

---

### 4.7 print() 完整签名与参数 {#47-print-签名}

| 项目 | 链接 |
|------|------|
| **题目** | [print() 函数的完整签名是什么？写出所有参数及其默认值](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.7） |
| **知识点** | [print 基本语法与参数说明](/python/python语法手册-04-格式化与输出) |
| **要点** | `print(*objects, sep=' ', end='\n', file=sys.stdout, flush=False)`。**参数**：`*objects` 要打印的内容；`sep` 分隔符默认空格；`end` 结尾符默认换行；`file` 输出目标默认标准输出；`flush` 是否立即刷新缓冲区默认 False。 |

---

### 4.10 print() 输出到文件 {#410-print-file}

| 项目 | 链接 |
|------|------|
| **题目** | [如何用 print() 将内容输出到文件而不是屏幕？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.10） |
| **知识点** | [print 的 file 参数：输出到文件](/python/python语法手册-04-格式化与输出) |
| **要点** | 使用 **`file`** 参数：`print("内容", file=文件对象)`。先 `open("文件名", "w", encoding="utf-8")` 得到文件对象，建议用 `with open(...) as f` 自动关闭。 |

---

### 4.12 repr() 与 print 输出区别 {#412-repr}

| 项目 | 链接 |
|------|------|
| **题目** | [repr() 的作用？print("apple\torange\n") 和 print(repr("apple\torange\n")) 的输出有什么不同？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.12） |
| **知识点** | [repr() 函数：官方字符串表示、显式不可见字符](/python/python语法手册-04-格式化与输出) |
| **要点** | **repr()**：返回对象「官方」字符串表示，便于调试；对字符串会**原样显示**转义符（如 `\t`、`\n`）。**区别**：`print(字符串)` 会**渲染** `\t`、`\n`（制表、换行）；`print(repr(字符串))` 输出带引号的字面形式 `'apple\torange\n'`，方便看清不可见字符。 |

---

### 4.13 __str__ 与 __repr__ 区别与受众 {#413-str-repr}

| 项目 | 链接 |
|------|------|
| **题目** | [__str__ 和 __repr__ 的区别是什么？分别面向什么受众？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.13） |
| **知识点** | [__str__ 与 __repr__：触发方式、受众、设计目标](/python/python语法手册-04-格式化与输出) |
| **要点** | **触发**：`__str__` → `print(obj)` / `str(obj)`；`__repr__` → 变量名、`repr(obj)`、容器中展示。**受众**：`__str__` 面向终端用户（可读）；`__repr__` 面向开发者/调试（准确、可还原）。**设计目标**：`__str__` 可读性好；`__repr__` 准确无误、最好能还原对象。 |

---

### 4.14 只定义 __repr__ 时 print(obj) 的行为 {#414-print回退repr}

| 项目 | 链接 |
|------|------|
| **题目** | [只定义 __repr__ 没有 __str__ 时，print(obj) 会发生什么？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.14） |
| **知识点** | [__str__ 默认回退到 __repr__](/python/python语法手册-04-格式化与输出) |
| **要点** | 会**回退调用 `__repr__`**。若未定义 `__str__`，Python 用 `__repr__` 的返回值作为 `print(obj)` 的输出。 |

---

### 4.15 列表中展示对象用 __str__ 还是 __repr__ {#415-容器中repr}

| 项目 | 链接 |
|------|------|
| **题目** | [print([obj]) 时调用的是 __str__ 还是 __repr__？](/python/Python核心语法自测试卷#四格式化输出-f-string-与-print-共-15-题)（自测卷 4.15） |
| **知识点** | [容器中展示默认用 __repr__](/python/python语法手册-04-格式化与输出) |
| **要点** | **`__repr__`**。在列表、元组等容器中展示元素时，Python 用 `__repr__` 来显示每个对象，便于调试和还原。 |

---

## 五、内置函数

### 5.2 max() / min() 的 key 参数 {#52-max-min-key}

| 项目 | 链接 |
|------|------|
| **题目** | [max() 和 min() 除了接受可迭代对象，还能接受什么参数来自定义比较逻辑？](/python/Python核心语法自测试卷#五内置函数-共-5-题)（自测卷 5.2） |
| **知识点** | [max() / min() 的 key 参数](/python/python语法手册-05-内置函数#53-max-和-min-最大值与最小值) |
| **要点** | **`key` 参数**。传一个函数（如 `key=lambda x: x["score"]`），对每个元素先算 `key(元素)`，再按这个返回值比大小。例：`max(students, key=lambda x: x["score"])` 按分数取最高分那条。 |

---

## 六、逻辑判断与真值检测

### 6.3 and 运算符的返回值规则 {#63-and返回值}

| 项目 | 链接 |
|------|------|
| **题目** | [and 运算符的返回值规则是什么？写出给定表达式的结果](/python/Python核心语法自测试卷#六逻辑判断与真值检测-共-10-题)（自测卷 6.3） |
| **知识点** | [and / or / not 的返回值规则](/python/python语法手册-06-逻辑与真值#61-逻辑运算符-and-or-not) |
| **要点** | **and**：返回**第一个假值**；若**全真**则返回**最后一个**。例：`1 and 2 and 3` → `3`；`0 and 2 and 3` → `0`；`"" and "hello"` → `""`。 |

---

### 6.4 or 运算符的返回值规则 {#64-or返回值}

| 项目 | 链接 |
|------|------|
| **题目** | [or 运算符的返回值规则是什么？写出给定表达式的结果](/python/Python核心语法自测试卷#六逻辑判断与真值检测-共-10-题)（自测卷 6.4） |
| **知识点** | [and / or / not 的返回值规则](/python/python语法手册-06-逻辑与真值#61-逻辑运算符-and-or-not) |
| **要点** | **or**：返回**第一个真值**；若**全假**则返回**最后一个**。例：`0 or "" or "hello"` → `"hello"`；`1 or 2 or 3` → `1`；`0 or "" or []` → `[]`。 |

---

### 6.5 被视为 False 的对象 {#65-假值列表}

| 项目 | 链接 |
|------|------|
| **题目** | [列出 Python 中所有被视为 False 的对象（至少写出 8 个）](/python/Python核心语法自测试卷#六逻辑判断与真值检测-共-10-题)（自测卷 6.5） |
| **知识点** | [真值测试：谁是“假”？](/python/python语法手册-06-逻辑与真值#62-真值测试谁是假-truth-value-testing) |
| **要点** | **常量**：`None`、`False`。**数值零**：`0`、`0.0`、`0j`。**空容器**：`''`、`[]`、`()`、`{}`、`set()`、`range(0)`。自定义对象：`__bool__()` 返回 False 或 `__len__()` 返回 0 的实例。 |

---

### 6.8 自定义对象真值判断优先级 {#68-真值优先级}

| 项目 | 链接 |
|------|------|
| **题目** | [Python 判断自定义对象真值的优先级顺序是什么？](/python/Python核心语法自测试卷#六逻辑判断与真值检测-共-10-题)（自测卷 6.8） |
| **知识点** | [自定义对象的真值：__bool__ / __len__ 优先级](/python/python语法手册-06-逻辑与真值#64-进阶自定义对象的真值) |
| **要点** | 1. **`__bool__()`**：最优先，按返回值定真/假。2. **`__len__()`**：若未定义 `__bool__`，看 `__len__()`，返回 0 则为假。3. **默认**：两者都未定义时，对象永远为 `True`。 |

---

## 七、身份判断与成员运算符

### 7.5 in 运算符的时间复杂度 {#75-in-时间复杂度}

| 项目 | 链接 |
|------|------|
| **题目** | [in 运算符在不同数据结构中的时间复杂度分别是多少？](/python/Python核心语法自测试卷#七身份判断与成员运算符-共-8-题)（自测卷 7.5） |
| **知识点** | [in / not in 不同结构的查找逻辑与复杂度](/python/python语法手册-07-身份与成员#721-不同结构的查找逻辑) |
| **要点** | **字符串**、**列表/元组**：O(n)（线性扫描）。**集合**、**字典（查键）**：O(1)（哈希表）。字典默认只查键；查值用 `in d.values()` 为 O(n)。 |

---

### 7.8 自定义类支持 in：__contains__ {#78-contains}

| 项目 | 链接 |
|------|------|
| **题目** | [如何让自定义类支持 in 运算符？需要定义什么方法？](/python/Python核心语法自测试卷#七身份判断与成员运算符-共-8-题)（自测卷 7.8） |
| **知识点** | [自定义对象支持 in：__contains__](/python/python语法手册-07-身份与成员#723-自定义对象支持-in) |
| **要点** | 在类中定义 **`__contains__(self, item)`** 方法；`item in obj` 时 Python 会调用 `obj.__contains__(item)`，返回 True/False。 |

---

## 九、字符编码

> 本章全部题目均为待复习，知识点统一见 [字符编码 语法手册](/python/python语法手册-09-字符编码)。

### 9.1 ASCII / Unicode / UTF-8 关系 {#91-编码铁三角}

| 项目 | 链接 |
|------|------|
| **题目** | [ASCII、Unicode、UTF-8 三者的关系是什么？分别负责什么？](/python/Python核心语法自测试卷#九字符编码-共-6-题)（自测卷 9.1） |
| **知识点** | [编码铁三角：ASCII / Unicode / UTF-8](/python/python语法手册-09-字符编码#91-编码铁三角的关系真相) |
| **要点** | **ASCII**：早期英文标准，1 字节。**Unicode**：全球码表，只分配码点（逻辑编号），不负责存储。**UTF-8**：编码方案，把 Unicode 码点转为变长字节流。 |

---

### 9.2 UTF-8 字节数 {#92-utf8-字节}

| 项目 | 链接 |
|------|------|
| **题目** | [UTF-8 中 ASCII 字符、汉字、Emoji 各占几字节？](/python/Python核心语法自测试卷#九字符编码-共-6-题)（自测卷 9.2） |
| **知识点** | [UTF-8 变长存储](/python/python语法手册-09-字符编码#91-编码铁三角的关系真相) |
| **要点** | ASCII 字符 **1 字节**，汉字 **3 字节**，Emoji **4 字节**。 |

---

### 9.3 str 与 bytes 互转 {#93-str-bytes}

| 项目 | 链接 |
|------|------|
| **题目** | [str 和 bytes 之间如何互相转换？写出编码和解码的代码](/python/Python核心语法自测试卷#九字符编码-共-6-题)（自测卷 9.3） |
| **知识点** | [编码 Encode / 解码 Decode](/python/python语法手册-09-字符编码#92-核心操作str-内存文本--bytes-硬盘字节) |
| **要点** | **编码**：`text.encode('utf-8', errors='replace')`（str → bytes）。**解码**：`byte_data.decode('utf-8', errors='replace')`（bytes → str）。 |

---

### 9.4 errors='replace' {#94-errors}

| 项目 | 链接 |
|------|------|
| **题目** | [errors='replace' 参数的作用是什么？](/python/Python核心语法自测试卷#九字符编码-共-6-题)（自测卷 9.4） |
| **知识点** | [encode/decode 的 errors 参数](/python/python语法手册-09-字符编码#92-核心操作str-内存文本--bytes-硬盘字节) |
| **要点** | 遇到无法编码/解码的字符时，用替代字符（如 �）代替，**防止整个程序崩溃**。 |

---

### 9.5 ord('中') 与 chr(65) {#95-ord-chr}

| 项目 | 链接 |
|------|------|
| **题目** | [ord('中') 的结果是什么？chr(65) 的结果是什么？](/python/Python核心语法自测试卷#九字符编码-共-6-题)（自测卷 9.5） |
| **知识点** | [ord() 与 chr()](/python/python语法手册-09-字符编码#93-核心转换工具ord-与-chr) |
| **要点** | `ord('中')` → **20013**（十进制码点）。`chr(65)` → **'A'**。 |

---

### 9.6 ord() 与 chr() 分别做什么 {#96-ord-chr}

| 项目 | 链接 |
|------|------|
| **题目** | [ord() 和 chr() 分别做什么？](/python/Python核心语法自测试卷#九字符编码-共-6-题)（自测卷 9.6） |
| **知识点** | [ord() 与 chr()](/python/python语法手册-09-字符编码#93-核心转换工具ord-与-chr) |
| **要点** | **ord(字符)**：输入单个字符，输出该字符的**十进制 Unicode 码点**。**chr(整数)**：输入十进制码点，输出对应的 **Unicode 字符**。 |

---

**相关**：[Python 核心语法自测试卷](/python/Python核心语法自测试卷) · [内置数据结构](/python/python语法手册-02-内置数据结构) · [异常](/python/python语法手册-03-异常) · [格式化与输出](/python/python语法手册-04-格式化与输出) · [内置函数](/python/python语法手册-05-内置函数) · [逻辑与真值](/python/python语法手册-06-逻辑与真值) · [身份与成员](/python/python语法手册-07-身份与成员) · [字符编码](/python/python语法手册-09-字符编码)
